# blackbox-theme-simple

A simple theme (light+dark) for the Black Box terminal.

## Installing

Simply just save the `simple-light.json` and `simple-dark.json` files
in `${XDG_DATA_HOME}/blackbox/schemes/`.
